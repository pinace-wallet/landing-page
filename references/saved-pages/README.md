# Saved reference pages

Drop your exported landing-page HTML here (and any other saved sites).

For each saved page, add a short note in `index.md`:
- file name
- source URL
- what we like about it (layout, motion, hero, section we want to borrow)

# GSAP

Skill source, animation recipes, and docs for GSAP.

**Role on this project:** motion layer for the high-impact moments only —
- Hero entrance + gradient animation
- "How it works" `propose_action → settle` flow
- The 1-click-revoke hero moment (STOP → `E_REVOKED` revert)
- Scroll-triggered section reveals

Keep motion selective; the dark/glass aesthetic reads premium when restrained.
